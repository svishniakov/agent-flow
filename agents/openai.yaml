interface:
  display_name: "Agent Flow"
  short_description: "Route work from idea to verified result"
  brand_color: "#2563EB"
  default_prompt: "Use $agent-flow only when the user starts the request with Agent Flow, $agent-flow, or agent-flow. Route that task through the smallest sufficient agent workflow and finish with verification evidence."

policy:
  allow_implicit_invocation: false
