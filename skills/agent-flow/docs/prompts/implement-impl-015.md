Agent Flow, реализуй полностью ОБА этапа implementation-плана 015:

<project-root>/skills/agent-flow/docs/implementation/impl-015-acceptance-lifecycle-recovery.md

Проект: <project-root> — абсолютный путь к текущему checkout Agent Flow.

Нужна реализация до принятого результата. Не составляй новый implementation-план и не останавливайся после этапа 1. Прочитай весь план, связанное исследование и архитектурное дополнение; используй актуальный установленный Agent Flow, его правила изоляции, регистрации доказательств и независимой приёмки. Сначала проверь состояние проекта, локальные инструкции, рабочую память и пересечения с другими задачами. Сохрани чужие изменения, прежний C5, архивы и рабочую глобальную установку.

Каждый работающий субагент обязан ДО работы выбрать, полностью прочитать и применить установленный профильный навык для своего поручения. Это относится ко всем ролям и повторным назначениям, включая QA и reviewer. Оркестратор передаёт проверенные пути SKILL.md; субагент записывает в handoff путь, назначение навыка и конкретные решения или проверки, в которых он применён. Общий Agent Flow и простое перечисление навыков не заменяют их применения. QA проверяет все назначения, reviewer подтверждает полноту. Если профильный навык недоступен после поиска, не объявляй его использованным, не устанавливай автоматически и продолжай независимую работу, указав зависимый блокер.

Этап 1: исправь общий механизм приёмки в единственном исходнике skills/agent-flow/ по L1–L6. Реализуй проверку перед атомарной финализацией, корректное различение текущих назначений и истории, защиту всех путей записи. Сохрани строгие проверки источников, моделей, независимости, результата и последнего завершённого хода. Выполни полный автоматический набор и отрицательные сценарии. Получи независимые QA и финальное ревью одной сохранённой редакции; исправляй технические замечания до принятия.

Этап 2: после проверки инструментов на изолированных копиях восстанови НАСТОЯЩИЙ заблокированный журнал:

<project-root>/.agent-work/runs/2026-09-14-codex-plugin-desktop-checks

Выполнение этого поручения включает применение проверенного восстановления к этому журналу. Сначала сохрани и проверь доступный канонический снимок. Используй штатный новый механизм и узкое legacy-исключение плана для исторического pass при текущем blocked. Не удаляй и не переписывай прежние события, отказы, UUID, timestamps, подготовленные входы или выводы агентов. Сохрани прежние bytes в архиве и исходный timeline как точный префикс. Не ослабляй strict_inputs и не удаляй ни один из пяти выбранных поведенческих критериев.

Доведи восстановление до свежей независимой приёмки прежнего неизменного C5, полной проверки текущего поколения и успешной штатной delivery. Одной команды reopen или успешного восстановления копии недостаточно. Приёмку нового общего runtime веди отдельно от приёмки прежнего C5; не выдавай старые Desktop-наблюдения за запуск изменённого кода.

Собери обе поставки из одного исходника: отдельный скилл и плагин Codex. Из готовых воспроизводимых архивов проверь установку и обновление в изолированных каталогах, сохраняя пользовательские файлы и настройки. Проведи реальные проверки новой процедуры приёмки для ОБЕИХ поставок в Desktop и CLI: плагин через @ в Desktop, отдельный скилл при выключенном плагине и соответствующие CLI-сценарии. Сохрани версии и хеши пакетов, источники вызовов, реальные сессии независимых QA/reviewer, результаты validate и delivery.

До клиентских прогонов проверь весь путь регистрации доказательств и заранее сохрани точные подготовленные входы. Если необходимы мои действия в Desktop, подготовь проекты и дай одну конкретную последовательность с точными текстами; сам продолжай независимую работу. Не повторяй прежние проверки ради оформления журнала. Отсутствие клиента, модели, навыка или обязательного доказательства не засчитывай как PASS или успешный skip.

Обнови документацию и фактические статусы. Заканчивай успешным результатом только когда выполнены L1–L6 и R1–R5, настоящий журнал восстановлен до принятия и delivery без изменения истории, обе поставки проверены и новая реализация принята независимыми QA/reviewer одной итоговой редакции. Иначе сохрани точный незакрытый критерий; не помечай оба этапа выполненными и продолжай доступную независимую работу.

Этапы 2 и 3 плана 011, pre-commit, GitHub Actions, вебхуки, публикация, ZCode и другие агенты вне задачи. Commit и push не делай.

В финале передай пути принятого результата и двух архивов, доказательства сохранности и восстановления настоящего журнала, таблицу L1–L6/R1–R5 и четырёх клиентских проверок, точную инструкцию установки/обновления и оставшиеся ограничения. Статус подготовки плана не считай выполнением реализации.
