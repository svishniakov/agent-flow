# CodeGraph fixtures

`scripts/test-codegraph.py` builds temporary git repositories for Python, TypeScript, and JavaScript fixture coverage. Static fixture docs live here so the CodeGraph test surface has a stable project-owned anchor.
